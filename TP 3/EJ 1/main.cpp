#include <stdio.h>
#include <iostream>
#include <vector>
#include <omp.h>

using namespace std;

int main(int argc, char **argv)
{
    int filas = 100;
    int columnas = 100;

    vector<vector<int>> matriz(filas, vector<int>(columnas, 1));
}